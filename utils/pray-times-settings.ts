export interface PrayTimesSettings {
    showAstronomicEvents?: boolean; // se deve exibir na UI os eventos astronômicos
    method?: string; // qual método de calculo a ser usado
    imsak?: string;
    dhuhr?: string;
    asr?: string;
    highLats?: string;
    midnight?: string;
    maghrib?: string;
    isha?: string;
    fajr?: string;
}